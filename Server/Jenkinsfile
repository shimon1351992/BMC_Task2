pipeline {
    agent {
       label 'slave'   
    }

    stages {
        stage('directory') {
            steps {
                echo 'Building..'
                bash """ls"""
            }
        }
          stage('build') {
            steps {
                bash "chmod +x -R ${env.WORKSPACE}";
                bash "./Server/Server.java"
            }
        }
        stage('Run') {
            steps {
              sh """
              cd Server
              java Server
              """
            }
        }
        
    
    
  
  } 
}
